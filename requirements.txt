# Motion Pro — Python dependencies
#
#   python -m venv .venv
#   .\.venv\Scripts\Activate.ps1    # Windows
#   pip install -r requirements.txt
#
# Firmware upload (after install):
#   cd firmware/imu_demo
#   python -m platformio run -t upload

# PlatformIO + ESP32 builds
platformio>=6.1.0

# Pre-build scripts (embed_textures.py — PNG → RGB565 headers)
Pillow>=10.0.0

# Wii Remote debug web tool (FastAPI, Bluetooth scan, etc.)
-r tools/wiimote_debug/backend/requirements.txt
